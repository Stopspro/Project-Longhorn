#![no_std]
extern crate learn;
extern crate log;
extern crate prefix;
extern crate nostd-horrorshow; // still a WIP

pub fn autocomplete() {
     prefix::sense::prefix();
     if prefix = 0;
          let filetype = "Normal Text";
     
     if prefix = 1;
          let filetype = "Rust";
          
     if prefix = 2;
          let filetype = "Python";
          
     if prefix = 3;
          let filetype = "HTML";
     
     currentthought = String::new();
     
     println!("{}", currentthought
          
}
