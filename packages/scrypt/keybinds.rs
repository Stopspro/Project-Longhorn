extern crate cpuio;

use cpuio::Port;

pub fn binds() {
  if visualmode = 1 {
    if letter = 1 { // A; no bind
      break
    }
    
    if 
  }
  
  if visualmode = 0 {
    println!("Error; visual mode not activated.")
  }
}
