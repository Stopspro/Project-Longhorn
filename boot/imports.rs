mod pack_mod {
  pub fn sql_setup() {
    let ionver =   //sql code call
    let electronver = 
    let neutronver = 
    let moleculever = 
    let chess = 
  }
  
  pub fn ionvar() {
    let ion = ionver;
    println!("starting ion setup...");
  }

  pub fn electronvar() {
    let electron = electronver; 
    println!("starting electron setup...");
  }

  pub fn neutron() {
    let neutron = neutronver; // honestly, this is currently unavailable
    println!("starting neutron setup...");
  }

  pub fn molecule() {
    let molecule = moleculever;
    println!("starting molecule setup...");
  }
    
  pub fn chess() {
    let chess = chessver
    println!
  }
