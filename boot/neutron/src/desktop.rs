mod desktop {
  fn load() { // this should pull desktop info from a text file
  
  }
  
  fn icons() { // this should load icons
  
  }
  
  fn cursor() { // this should create a cursor
  
  }
  
  fn terminal() { // this should create a terminal session
  
  }
  
}
